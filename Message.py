from tkinter import messagebox  # Import the messagebox module from tkinter for displaying message dialogs

def show_message(title, message):
    # Display an informational message box with a specified title and message
    messagebox.showinfo(title, message)  # Show an info dialog with the given title and message